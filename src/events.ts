export namespace Events {
  export enum Widget {
    Loaded = 'widget.loaded',
  }

  export enum TaskViewer {
    ParentRouteChanged = 'task-viewer.parent-route-changed',
    RouteNotConfigured = 'task-viewer.route-not-configured',
  }
}
